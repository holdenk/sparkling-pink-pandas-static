[tox]
envlist = py3, lint

[testenv]
deps =
    pytest
    pytest-cov
    pyyaml
    Pillow
commands =
    pytest --cov=scripts --cov-report=term-missing --cov-report=html {posargs}

[testenv:lint]
deps =
    ruff
commands =
    ruff check scripts/ tests/
